import { OcticonsDirective } from './octicons.directive';

describe('OcticonsDirective', () => {
  it('should create an instance', () => {
    const directive = new OcticonsDirective();
    expect(directive).toBeTruthy();
  });
});
